{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "preview page": "nuxt build; wrangler pages dev dist/",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230518.0",
    "@nuxt/devtools": "latest",
    "@nuxt/image": "^1.5.0",
    "@types/node": "^18",
    "nuxt": "^3.11.1"
  },
  "dependencies": {
    "firebase": "^9.22.2",
    "vue-icomoon": "^0.6.1"
  }
}
